#compdef pj

local default_host=${PJ_DEFAULT_HOST:-"github.com"}

[[ -n $PJ_DEFAULT_USER ]] && \
	_files -W "$CODEPATH/$default_host/$PJ_DEFAULT_USER" -/

_files -W "$CODEPATH/$default_host" -/

_files -W "$CODEPATH" -/
